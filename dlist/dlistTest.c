#include <stdio.h>
#include <stdlib.h>

#include "dlist.c"

void create(int data) {
  int *value = malloc(sizeof *value);
  *value = data;
  return value;
}

int main(int argc, char [] argv) {
  int
}
